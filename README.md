# day-1-p-Git
Day 1 practicing Git &amp; GitHub

Git day01
----
git account creation
login to git
create an repo public/private
open vs code and choose folders

check git is installed or not in locally
git -v

configure your accout to git
git config --global user.email "example@gmail.com"
git config --global user.name "username"

To check the config details
git config --list

Linux Cmds
Ls, ll,
PWD
touch
mkdir
rm --> remove file
rmdir --> remove directory
rm -rf --> removes forcfully without any promt
cd --> changfe directory
rm -r folder_name

git init  -- initilize an empty repo
git clone https://github.com/devops.proorg/day-1-p-github.git

git status ---> to check the status of the git changes we made

git add . ---> to add the chnages to
git commit -m "commit message"

